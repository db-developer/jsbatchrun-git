env:
  global:
    - CODECOV_TOKEN="todo" # https://codecov.io

language: node_js

node_js:
  - 10

# not recuired as of 2016(?)
# before_script:
#  - npm install -g grunt-cli

before_script:
  - npm install -g codecov

script:
  - npm run all      # see package.json scripts or gruntfile.js
  - codecov          # upload coverage reports to codecov.io
